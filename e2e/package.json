{
  "name": "afspaces-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-End-Tests für Asgaros Forum Spaces (Playwright).",
  "scripts": {
    "test": "playwright test",
    "test:nojs": "playwright test --project=no-js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1"
  }
}
